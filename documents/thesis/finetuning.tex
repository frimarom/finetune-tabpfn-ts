\chapter{Finetuning TabPFN-TS}
\label{chap:finetuning}
TabPFN-TS has already showed remarkable results on the GIFT-Eval benchmark.
However, as illustrated in Figure~\ref{fig:hospital_tabpfn_ts_pred}, it still struggles especially with noisy and non-periodic time series.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{pictures/hospital.png}
    \caption{Prediction by TabPFN-TS for the hospital dataset. As shown, the time series is very noisy and non-periodic; therefore, the forecast can only capture the overall trend. Shaded regions represent the 80\%, 60\%, and 40\% prediction intervals, where wider intervals indicate higher uncertainty and higher coverage.}
    \label{fig:hospital_tabpfn_ts_pred}
\end{figure}
To address these limitations, we investigate whether finetuning can improve performance on such challenging datasets.
In particular, we evaluate two finetuning strategies with the datasets of GIFT-Eval and autogluon/chronos-datasets.
The first involves finetuning on a single dataset, which is expected to improve performance on that dataset and potentially on others with similar characteristics.
The second strategy involves finetuning on multiple datasets.
As shown by \citet{Hollmann_2025}, finetuning TabPFN across multiple datasets is equivalent to continued pretraining, which can improve performance across a wide range of datasets.
We extend this approach to time series data.

\section{Experimental Setup}
%Zuerst daten vorbereitung(dataset_utils)
%daten pro training dataloader
%validation
%hyperparameter setup
%evaluation
For our experiments, we will use full supervised finetuning in which we update all parameters of the model.
This method has been shown to be the most effective way of finetuning TabPFN, as the model tolerates full updates well and has a relatively small number of parameters. \citep{tanna2026exploring}.

For the most part, the code that is used will be provided by Lennart Purucker's repository ``finetune\_tabpfn\_v2'' \footnote{\url{https://github.com/LennartPurucker/finetune_tabpfn_v2}}.
Although most of the setup was completed with it, we still had to adjust the normalization of the training error to first make the script work for normal tabular data.
To let the script also finetune on time series data, we also had to make some adjustments in the data creation, batch creation, and validation.

\subsubsection{Data Creation and Transformation}
GIFT Eval contains 23 datasets, each comprising varying numbers of time series from different domains.
For each dataset, a forecast horizon is defined based on its frequency.
The frequency indicates how often observations are recorded, for example, daily, weekly, or monthly.\newline
Based on this and a predetermined number of sliding windows, training, validation, and test sets can be created for each time series in the dataset.
The test and validation splits have non-overlapping forecast targets to ensure that evaluation and validation are performed on unseen data not included during training.
This means that for a time series with $n$ values, a forecast horizon of $m$ and $w$ windows the training split contains the first $n-(m \cdot (w+1))$ observations, the validation split contains the first $n-(m \cdot w)$ observations and the test split contains all values with the last $(m \cdot w)$ being split into $w$ windows for benchmark evaluation.
This can be seen in the Appendix in Figure~\ref{fig:gift_eval_split_us_births_m}.\newline
While only the test data is split into multiple sliding windows, we implemented the training data to also be split into $w$ windows.
This is done with the same method as the test split for each time series in a dataset, thus resulting in more training data.

To finetune TabPFN with time series data in the form of tabular data, we have to transform the data that is selected.
Every time series is transformed into 28 features containing the running index, calendar features, and automatic season features.
This will be done with the code provided by \citet{hoo2025tables} as it was explained in \autoref{chap:background}.
\newline
Datasets with time series that do not have a constant length are not taken into account since they require padding to be useful for uniform batch creation.
\newline
Since TabPFN-TS was evaluated on GIFT-Eval using a maximum of 4096 points as context length, and for computational purposes, we also clipped all time series to this length.

GIFT-Eval supports three different prediction lengths for almost every dataset, e.g., \ short, medium, and long.
This is done by multiplying the original forecast horizon by different factors that are also based on the frequency of the dataset.
Note that we will only finetune using the short (so the original) forecast horizon, as extending it would exceed the available computational resources.

\subsubsection{Data Loading}

As explained in \autoref{chap:background}, instead of $(x, y)$, one sample consists of a set of n tuples with the form $(x_i, y_i)$.
So in training, one batch consists of $k$ samples, with $k$ also known as the batch size.

The selection of training and validation data for time series fundamentally differs from that of tabular data.
For tabular data, before finetuning, one typically randomly selects $n$ samples from the dataset and removes them from the validation set.
Training samples are drawn in a similar way, but remain part of the original dataset.

For time series, the procedure is different.
Each dataset consists of multiple time series, and, as stated earlier, each time series is divided into training, validation, and test splits.

To create a validation set of $k$ predictions, $k$ time series are randomly selected, and their validation splits are used.
After that, we validate every $n$ steps and take the mean of all validation losses of each validation time series.
This validation loss is also used for early stopping, which we configured with a minimum patience of 100 and a maximum patience of 200.

To construct a training batch, one window is randomly sampled from a randomly chosen time series in the current dataset.
This process is repeated $k$ times to create a batch of $k$ training samples for one epoch.
The same sampling procedure is repeated in every epoch.

\subsubsection{Hyperparameters}
In addition to the standard hyperparameters such as batch size and learning rate, we also use a regularizer for the distance to the L2-Starting Point (L2-SP) \citep{xuhong2018explicit}.
Instead of a normal L2 distance, we penalize large deviations from the initial pretrained weights of the model $w^0$.
It is calculated via
\[
    \Omega(w)= \frac{\alpha}{2} \lVert w - w^0 \rVert
\]
where $\alpha$ is the regularization parameter setting the strength of the penalty, thus representing a hyperparameter.
\citet{Hollmann_2025} used it for finetuning with multiple datasets, and we wanted to see whether it also improves performance in our experiments

Instead of doing a hyperparameter search first to determine which hyperparameters perform best, we finetune TabPFN 54 times on one dataset for each different hyperparameter configuration.
We use batch sizes 2, 4, 8, 16, 32, and 64 as well as learning rates 0.0005, 0.00005, and 0.000005.
As $\alpha$ for the L2-SP Norm, we use 0, 0.003, and 0.0002.

After that, we take the 5 finetuning configurations with the best validation loss and evaluate on the GIFT-Eval benchmark.

For validation loss we use the Mean Squared Error (MSE).
\subsubsection{Evaluation}
For evaluation, we use the script provided by TabPFN-TS and only evaluate on short forecast horizons.
Note that we were not able to reproduce all benchmark results that are currently submitted in GIFT-Eval, so before finetuning, we evaluate every dataset on the base configuration and take it as a reference.
For the base configuration, we use the \textit{tabpfn-v2-regressor-2noar4o2} configuration, which was also used in \citet{hoo2025tables}.

To compare the evaluation results, we observe the Mean Absolute Scaled Error (MASE) and take the percentage difference compared to the original configuration, and average it for the five finetuning configurations.
If this value is negative, we conclude an improvement.
If it is positive, we conclude a worsening.
\section{Results}
\label{sec:results_finetuning}
This section will present the results of our finetuning experiments, while not interpreting them. %TODO
\subsection{Finetuning on one Dataset}
\label{subsec:singlefinetuning}
For finetuning on one dataset, we, as the name suggests, only used data from one dataset.
Since many datasets from GIFT-Eval come with different observation frequencies, we use only the data from one frequency from one dataset, as it was divided in GIFT-Eval.
With this method, we were able to train TabPFN on 33 different datasets with different metadata like context length, time series amount, or forecast horizon.
After completing this procedure, we now have 54 configurations for each of the 33 datasets.
This is due to our method of evaluating 54 different configurations for hyperparameter tuning.

We report a brief training summary of the best configurations, selected based on validation loss, for each dataset in Figure~\ref{tab:single_finetuning_summary_val} in the Appendix.
The results show that the validation loss improved in at least one of the configurations for all but two datasets.
The two datasets that did not improve regarding the validation loss are \textit{jena\_weather} with frequency \textit{10T} and \textit{ett1} with frequency \textit{D}, both of which are multivariate datasets with 20 and 7 varieties.

%To evaluate whether the finetuning process improved or degraded the performance of TabPFN-TS on our dataset, we rank all 54 configurations for each dataset according to their validation loss and select the top five.
%This will result in five lists of different error metrics for each dataset, from which we will select the MASE as we mentioned earlier, and the percentage difference to its original configuration.
We further evaluated the Top-5 configurations according to validation loss and compared the percentage difference in MASE to the original configuration.

The results in Figure~\ref{tab:single_finetune_wide} in the Appendix show that only 14 finetuning runs led to an improved average performance for every dataset.
\newline
Another observation from this figure is that the ordering of configurations selected based on validation loss does not match their ordering in evaluation performance.
Not only that, but it also does not always indicate an improvement on the benchmark regarding the dataset if the validation loss itself improved.

\newpage
An example can be seen in Figure~\ref{fig:eval_M_DENSE_H} by looking at the five evaluated configurations of the dataset \textit{M\_DENSE} with hourly frequency.
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.5]{pictures/eval_M_DENSE_H.png}
    \caption{Evaluation results of finetuning with \textit{M\_DENSE} dataset with hourly frequency. The picture shows an improvement and worsening of the top five configurations.}
    \label{fig:eval_M_DENSE_H}
\end{figure}

\begin{figure}[H]
    \centering

    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{pictures/plot1_score_series.pdf}
        \caption{Score Series}
        \label{fig:plot1_score_series}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{pictures/plot2_context_vs_series.pdf}
        \caption{Context vs Series}
        \label{fig:plot2_context_vs_series}
    \end{subfigure}

    \caption{Listing of both plots regarding the context length and time series amount. Green marks an improvement, while red indicates performance degradation.}
    \label{fig:combined_plots}
\end{figure}
To further investigate our results, we visualize the results with respect to task characteristics in Figure~\ref{fig:plot1_score_series}.
This plot shows the relationship between the total number of observations per dataset and the percentage MASE difference compared to the score of the original model.
For datasets with more than 100,000 observations, the MASE difference is predominantly negative.

Figure~\ref{fig:plot2_context_vs_series} illustrates that datasets with both high context length and many time series are predominantly associated with negative MASE differences.
%If there is a notable amount of time series and a notable amount of context finetuning was successful and improved performance.
Datasets with only one of these characteristics are more frequently associated with positive MASE differences.
This can be observed particularly in datasets containing only a single time series, which, with few exceptions, perform worse than before finetuning.

All 54 finetuning runs for every dataset can be viewed in the attached repository \footnote{\url{https://github.com/frimarom/finetune-tabpfn-ts}}.

\subsection{Finetuning on Multiple Datasets}
\label{subsec:multifinetuning}
Next, we finetune TabPFN on multiple time series datasets simultaneously.
This is done by first selecting a handful of datasets we want to use for training, and then for every epoch randomly selecting one dataset we want to build the batch from.
For the next epoch, data from another dataset will be drawn.

Validation is performed on arbitrarily but fixed validation splits from each training dataset, with errors normalized and aggregated to obtain a combined validation metric.

Instead of finetuning TabPFN on a large amount and variety of datasets, we decided to split the datasets into three categories with respect to different context lengths, motivated by \citet{Hollmann_2025}.
They concluded that a longer context resulted in better results, and we wanted to validate these results.
\newline
We defined the threshold for each category as follows: Small $\rightarrow$ 0-240, Medium $\rightarrow$ 240-1000, Large $\rightarrow$ 1000-Inf.
Note that all time series with a context length greater than 4096 were truncated, as 4096 is the largest context length considered during training, following \citet{hoo2025tables}.

We evaluate our finetuned models in three settings:
The unseen test sets of the datasets it was trained on, unseen datasets from the same category, and unseen datasets from different categories.
%For independent evaluation, we are limited to datasets we did not finetune with.
%This limits most of the categories to only a few datasets.
%So for our plan, we also evaluate on datasets we trained with and datasets from other categories to check if performance improves across the board.
%Both of these are marked in the plots.
\newpage
\subsubsection{Small Context Length}
For datasets with a short context length (0–240 observations per time series), we selected the datasets visible in Table~\ref{tab:datasets_combined} for training.
Since only a few datasets fall into this category, we were able to independently evaluate the configurations on only a single dataset.
\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{llrrr}
\hline
Split & Dataset & Context Length & Num Series & Source \\
\hline
\multirow{5}{*}{Train}
& ett1/W         & 103 & 1   & GIFT-Eval \\
& hospital       & 84  & 767 & GIFT-Eval \\
& electricity/W  & 208 & 370 & GIFT-Eval \\
& covid\_deaths  & 212 & 266 & GIFT-Eval \\
& ett2/W         & 103 & 1   & GIFT-Eval \\
& monash\_car\_parts & 51 & 2674 & autogluon/chronos-datasets \\
& monash\_nn5\_weekly & 113 & 111 & autogluon/chronos-datasets \\
\hline
\multirow{1}{*}{Evaluation}
& solar/W        & 52  & 137 & GIFT-Eval \\
\hline
\end{tabular}
}
\caption{}
\label{tab:datasets_combined}
\end{table}
In addition to this and as stated above, we will also evaluate on the datasets we trained on, as well as two datasets that are from the medium and large categories, see Table~\ref{tab:selected_datasets}.
\begin{table}[H]
\centering
\begin{tabular}{lrrrr}
\hline
Dataset & Context Length & Num Series & Category & Source \\
\hline
LOOP\_SEATTLE/D & 365 & 323 & Medium & GIFT-Eval \\
M\_DENSE/H & 17520 & 30 & Large & GIFT-Eval \\
\hline
\end{tabular}
\caption{}
\label{tab:selected_datasets}
\end{table}
% first run
% list dataset we finetuned with
% list dataset we evaluated with and results
% list datasets we also trained with
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{pictures/multi_short/mase_mean_delta_plot.png}
    \caption{Horizontal diverging bar chart for multi-dataset finetuning with short context length. Lower is better.}
    \label{fig:multi_short_mase_mean_delta_plot}
\end{figure}
\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{l l c r r r r r r}
\hline
dataset & context & trained & ckpt1 & ckpt2 & ckpt3 & ckpt4 & ckpt5 & mean \\
\hline
m\_dense/H
& large & $\times$
& 18.25 & 36.51 & 15.43 & \cellcolor{yellow}2.53 & 7.19
& 15.98 \\

loop\_seattle/D
& medium & $\times$
& 5.87 & 10.17 & 4.17 & \cellcolor{yellow}\textbf{-1.35} & 2.55
& 4.28 \\

solar/W
& short & $\times$
& \cellcolor{yellow}\textbf{-1.27} & -0.07 & 2.93 & 3.76 & 44.53
& 9.98 \\

hospital/M
& short & \checked
& 7.12 & 0.92 & 5.24 & \cellcolor{yellow}\textbf{-0.46} & 0.56
& 2.68 \\

electricity/W
& short & \checked
& 4.44 & 8.86 & 6.01 & \cellcolor{yellow}\textbf{-2.60} & 4.09
& 4.16 \\

ett1/W
& short & \checked
& -11.15 & -1.99 & \cellcolor{yellow}\textbf{-13.80} & -9.13 & -11.30
& -9.47 \\

covid\_deaths/D
& short & \checked
& 23.00 & 68.43 & \cellcolor{yellow}5.89 & 16.77 & 19.65
& 26.75 \\

ett2/W
& short & \checked
& 97.35 & 81.49 & 45.00 & \cellcolor{yellow}44.82 & 97.17
& 73.16 \\
\hline
\end{tabular}
}
\caption{Results of the five configurations for multi-dataset finetuning with short context length. The values correspond to the percentage difference of the trained configurations from the original configuration. If a configuration resulted in an improvement (negative value), it is shown in bold when it also corresponds to the smallest value.}
\label{tab:multi_short_wide_results}
\end{table}
We report results in Figure~\ref{fig:multi_short_mase_mean_delta_plot} and Table~\ref{tab:multi_short_wide_results}.
As the graph depicts, the mean performance of all 5 best configurations performed significantly worse than the original configuration on almost all datasets.
The MASE not only worsened on the designated evaluation dataset but also worsened on the dataset we trained with, as well as the two datasets that came from a different category.
The only exception is \textit{ett1/W}, where we can see a small average improvement of 1,99\%.
This behavior can be observed across all top 5 configurations.
So, all five configurations performed almost the same.

\subsubsection{Medium Context Length}
For medium-sized datasets, there was more data available, thus more data to independently evaluate TabPFN on.
But first, for training and independent evaluation, we chose the datasets shown in Table~\ref{tab:ctx_ts_combined} with their context length and time series amount.
\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{llrrr}
\hline
Split & Dataset & Context Length & Num Series & Source \\
\hline
\multirow{6}{*}{Train}
& ett2/D                 & 725 & 1   & GIFT-Eval\\
& sz\_taxi/H             & 744 & 156 & GIFT-Eval\\
& us\_births/M           & 240 & 1   & GIFT-Eval\\
& solar/D                & 365 & 137 & GIFT-Eval\\
& ett1/D                 & 725 & 1   & GIFT-Eval\\
& m\_dense/D             & 730 & 30  & GIFT-Eval\\
& monash\_fred\_md         & 728 & 107 & autogluon/chronos-datasets \\
& nn5                    & 791 & 111 & autogluon/chronos-datasets \\
\hline
\multirow{4}{*}{Evaluation}
& bitbrains\_rnd/H/short & 720 & 500 & GIFT-Eval \\
& loop\_seattle/D        & 365 & 323 & GIFT-Eval \\
& saugeenday/M           & 780 & 1   & GIFT-Eval \\
& hierarchical\_sales/W  & 260 & 118 & GIFT-Eval \\
& jena\_weather/D         & 366 & 1 & GIFT-Eval \\
\hline
\end{tabular}
}
\caption{}
\label{tab:ctx_ts_combined}
\end{table}
And to check whether performance also improved on datasets that were not even in the category, we chose the datasets listed in Table~\ref{tab:ctx_ts_3}.
\begin{table}[H]
\centering
\begin{tabular}{lrrrr}
\hline
Dataset & Context Length & Num Series & Category & Source \\
\hline
sz\_taxi/15T & 2976 & 156 & Large & GIFT-Eval \\
covid\_deaths/D & 212 & 266 & Small & GIFT-Eval \\
\hline
\end{tabular}
\caption{}
\label{tab:ctx_ts_3}
\end{table}
After training, we got the following results over all evaluated datasets in Figure~\ref{fig:multi_medium_mase_mean_delta_plot}.
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{pictures/multi_medium/mase_mean_delta_plot.png}
    \caption{Horizontal diverging bar chart of multi-dataset finetuning with short context length. Lower is better.}
    \label{fig:multi_medium_mase_mean_delta_plot}
\end{figure}
The results are completely different from those of short context finetuning.
\newline
What is immediately apparent is that, on average across all five configurations, 8 out of 13 datasets show a performance improvement.
On all datasets, which we only used for evaluation, TabPFN performed better than the original configuration.
Contrary to that, 7 out of 12 datasets we trained TabPFN with experienced a performance loss in the evaluation.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{l l c r r r r r r}
\hline
dataset & context & trained & ckpt1 & ckpt2 & ckpt3 & ckpt4 & ckpt5 & mean \\
\hline
covid\_deaths/D
& short & $\times$
& 7.91 & 9.15 & 14.74 & 18.53 & \cellcolor{yellow}27.55
& 15.58 \\

sz\_taxi/15T
& large & $\times$
& \cellcolor{yellow}\textbf{-0.83} & -0.71 & -0.53 & 0.69 & 0.31
& -0.22 \\

bitbrains\_rnd/H
& medium & $\times$
& -0.93 & -0.92 & -1.73 & \cellcolor{yellow}\textbf{-3.00} & -1.91
& -1.70 \\

loop\_seattle/D
& medium & $\times$
& \cellcolor{yellow}\textbf{-2.83} & -2.52 & -1.91 & -0.38 & -0.08
& -1.54 \\

saugeen/M
& medium & $\times$
& 0.60 & -0.50 & 1.58 & -0.43 & \cellcolor{yellow}\textbf{-3.49}
& -0.45 \\

hierarchical\_sales/W
& medium & $\times$
& \cellcolor{yellow}\textbf{-1.16} & -0.92 & -0.55 & 1.02 & 0.13
& -0.30 \\

jena\_weather/D
& medium & $\times$
& -2.40 & -3.42 & \cellcolor{yellow}\textbf{-5.94} & 8.37 & 6.94
& 0.71 \\

ett2/D
& medium & \checked
& -34.85 & \cellcolor{yellow}\textbf{-35.17} & -34.10 & -22.96 & -29.19
& -31.26 \\

sz\_taxi/H
& medium & \checked
& -1.89 & \cellcolor{yellow}\textbf{-1.90} & -1.57 & -0.18 & 0.36
& -1.04 \\

us\_births/M
& medium & \checked
& -5.32 & \cellcolor{yellow}\textbf{-14.80} & -7.64 & 0.94 & 24.62
& -0.44 \\

solar/D
& medium & \checked
& 1.01 & 1.02 & \cellcolor{yellow}\textbf{-2.72} & 0.57 & 0.46
& 0.07 \\

ett1/D
& medium & \checked
& 1.27 & \cellcolor{yellow}0.17 & 6.21 & 1.18 & 2.23
& 2.21 \\

m\_dense/D
& medium & \checked
& \cellcolor{yellow}\textbf{-0.52} & 0.00 & 1.65 & 4.04 & 5.94
& 2.22 \\

\hline
\end{tabular}
}
\caption{Results of the five configurations for multi-dataset finetuning with medium context length.}
\label{tab:medium_context_wide_results}
\end{table}
By looking at Table~\ref{tab:medium_context_wide_results}, we can see that configurations 1 and 2 perform best out of the 5 configurations we evaluated on.
Configurations 4 and 5 performed worse and only improved performance on 5 and 4 datasets.
All configurations experienced a deterioration on the small context length dataset.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{l r r r r r r r r r r r l r r}
\hline
ckpt & Time (s) & LR & BS & L2-SP & WD & TimeTot & InitLoss & BestLoss & LastLoss & Steps & BestStep & ES Reason & t/step (s) & Util \\
\hline
ckpt\_1 & 2400 & 5e-05 & 32 & 0.0 & 0.0 & 531.13 & 0.3873 & 0.3327 & 0.3361 & 470 & 259 & AdaptiveES & 1.13 & 98.78 \\
ckpt\_2 & 2400 & 5e-05 & 32 & 0.0001 & 0.0 & 513.75 & 0.3873 & 0.3330 & 0.3409 & 450 & 249 & AdaptiveES & 1.14 & 98.84 \\
ckpt\_3 & 2400 & 5e-05 & 8 & 0.0001 & 0.0 & 267.59 & 0.3873 & 0.3352 & 0.3511 & 520 & 309 & AdaptiveES & 0.51 & 88.65 \\
ckpt\_4 & 2400 & 0.0005 & 32 & 0.0001 & 0.0 & 564.65 & 0.3873 & 0.3358 & 0.3632 & 500 & 299 & AdaptiveES & 1.13 & 98.67 \\
ckpt\_5 & 2400 & 0.0005 & 16 & 0.0015 & 0.0 & 343.30 & 0.3873 & 0.3360 & 0.3412 & 490 & 289 & AdaptiveES & 0.70 & 96.84 \\
\hline
\end{tabular}
}
\caption{Training Runs of the top 5 configurations of multi finetuning with datasets that have a medium size context length.}
\label{tab:medium_context_training_ckpts}
\end{table}

The hyperparameters are also an interesting topic to look at.
Table~\ref{tab:medium_context_training_ckpts} showed that 4 out of 5 configurations used the L2-SP lambda also used in \citet{Hollmann_2025}.
Configurations 4 and 5 used a learning rate of 0.0005, contrary to configurations 1-3, which used 0.00005.

% im mean wird es auf 8 besser und auf 4 schlechter
% auf 3 datensets auf denen auch trainiert wurde wurde es schlechter
% auf datensets auf denen nicht trainiert wurde und nur evaluiert wurde und auch medium length haben wurde es nur besser bzw ziemlich gleich
%
% configuration 2 performed am besten über alle datensets
% configurations 4 und 5 performen schlechter als der durchschnitt
% auf short viel verschlechtert und auf big context length nicht signifikant verbessert aber auch nicht verschlechtert
% bei hyperparametern sind l2 sp lambda in 4/5 configurations drin
% da ckpt 4 und 5 schlechter performen und lr 0.0005 haben schlechter im vergleich zu 0.00005
%
\subsubsection{Large Context Length}
% list dataset we finetuned with
% list the dataset we evaluated with
% list datasets we also trained with
The last multi-finetuning category is the large context length category.
All datasets in this category consist of time series with a context length of at least 1000 observations.
So, for training and independent evaluation, we chose the ones listed in Table~\ref{tab:ctx_ts_combined_2}.
\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{llrrr}
\hline
Split & Dataset & Context Length & Num Series & Source \\
\hline
\multirow{4}{*}{Train}
& hierarchical\_sales/D & 1825  & 118 & GIFT-Eval \\
& electricity/D         & 1461  & 370 & GIFT-Eval \\
& sz\_taxi/15T          & 2976  & 156 & GIFT-Eval \\
& m\_dense/H            & 17520 & 30  & GIFT-Eval \\
\hline
\multirow{6}{*}{Evaluation}
& saugeen/D/short    & 23741 & 1 & GIFT-Eval \\
& us\_births/D/short    & 7305  & 1 & GIFT-Eval \\
& ett1/15T              & 69680 & 1 & GIFT-Eval \\
& ett1/H                & 17420 & 1 & GIFT-Eval \\
& ett2/H                & 17420 & 1 & GIFT-Eval \\
& ett2/15T              & 69680 & 1 & GIFT-Eval \\
& bizitobs\_application/10S & 8834 & 1 & GIFT-Eval \\
& bizitobs\_service/10S     & 8835 & 21 & GIFT-Eval \\
& jena\_weather/10T & 52704 & 1 & GIFT-Eval \\
& jena\_weather/H & 8784 & 1 & GIFT-Eval \\
& saugeen/W & 3391 & 1 & GIFT-Eval \\
& us\_births/W & 1043 & 1 & GIFT-Eval \\

\hline
\end{tabular}
}
\caption{}
\label{tab:ctx_ts_combined_2}
\end{table}
To evaluate whether the trained configurations also improved on datasets that are not in the same category (so they have fewer observations than 1000), we also evaluate on the datasets listed in Table~\ref{tab:ctx_ts_6}.
\begin{table}[H]
\centering
\begin{tabular}{lrrrr}
\hline
Dataset & Context Length & Num Series & Category & Source \\
\hline
covid\_deaths/D & 212 & 266 & Small & GIFT-Eval\\
loop\_seattle/D & 365 & 323 & Medium & GIFT-Eval\\
\hline
\end{tabular}
\caption{}
\label{tab:ctx_ts_6}
\end{table}
\newpage
Our evaluation emitted the following results:
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{pictures/multi_big/mase_mean_delta_plot.png}
    \caption{Horizontal diverging bar chart of multi-dataset finetuning with short context length. Lower is better.}
    \label{fig:multi_medium_mase_mean_delta_plot}
\end{figure}
8 out of 18 datasets show improved average results over all configurations compared to the original configuration.
But on most of these datasets, the improvement is not as significant as that of others.
Looking at the evaluation datasets, we see that 5 out of 12 datasets show an improvement compared to the original.

The short dataset also showed a performance degradation compared to the original while the medium-sized dataset experienced a performance improvement.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{l l c r r r r r r}
\hline
dataset & context & trained & ckpt1 & ckpt2 & ckpt3 & ckpt4 & ckpt5 & mean \\
\hline
covid\_deaths/D
& short & $\times$
& 23.56 & 77.34 & 49.68 & 49.19 & \cellcolor{yellow}44.23
& 48.80 \\

loop\_seattle/D
& medium & $\times$
& -1.92 & -1.96 & \cellcolor{yellow}\textbf{-2.80} & -2.62 & -2.07
& -2.27 \\

ett1/15T
& large & $\times$
& -2.98 & -5.00 & \cellcolor{yellow}\textbf{-7.94} & -7.19 & -5.88
& -5.80 \\

ett1/H
& large & $\times$
& -0.03 & \cellcolor{yellow}\textbf{-4.94} & -2.68 & -2.64 & -2.60
& -2.58 \\

saugeen/D
& large & $\times$
& 1.82 & -2.55 & \cellcolor{yellow}\textbf{-4.33} & -2.01 & 2.08
& -1.00 \\

ett2/H
& large & $\times$
& -1.79 & \cellcolor{yellow}\textbf{-4.05} & -0.47 & -0.81 & 2.61
& -0.90 \\

ett2/15T
& large & $\times$
& 1.77 & -0.42 & \cellcolor{yellow}\textbf{-1.67} & -1.36 & 0.20
& -0.30 \\

us\_births/D
& large & $\times$
& 34.64 & 20.01 & 20.29 & 20.01 & \cellcolor{yellow}19.44
& 22.89 \\

saugeen/W
& large & $\times$ & 2.58 & -0.68 & \cellcolor{yellow}\textbf{-1.14} & 0.072 & -0.52
& 0.062 \\

jena\_weather/10T
& large & $\times$ & 15.59 & 8.81 & \cellcolor{yellow}2.73 & 3.08 & 5.08
& 7.06 \\

jena\_weather/H
& large & $\times$ & 11.45 & 6.30 & \cellcolor{yellow}5.17 & 5.81 & 7.43
& 7.23 \\

us\_births/W
& large & $\times$ & 56.39 & 55.16 & \cellcolor{yellow}8.50 & 12.59 & 16.31
& 29.79 \\

us\_births/D
& large & $\times$ & 34.64 & 20.01 & 20.01 & \cellcolor{yellow}19.44 & 20.30
& 22.87 \\

bizitobs\_service/10S
& large & $\times$ & 33.17 & \cellcolor{yellow}12.01 & 22.92 & 16.30 & 34.20
& 23.72 \\

hierarchical\_sales/D
& large & \checked
& -1.35 & -1.44 & -0.33 & -0.30 & \cellcolor{yellow}\textbf{-1.62}
& -1.01 \\

electricity/D
& large & \checked
& 1.65 & \cellcolor{yellow}\textbf{-3.18} & -0.77 & -0.63 & -0.45
& -0.68 \\

sz\_taxi/15T
& large & \checked
& 0.05 & \cellcolor{yellow}\textbf{-0.27} & 0.20 & 0.22 & 0.05
& 0.05 \\

m\_dense/H
& large & \checked
& 1.01 & -1.63 & 2.12 & 2.10 & \cellcolor{yellow}\textbf{-2.21}
& 0.28 \\
\hline
\end{tabular}
}
\caption{Results of the five configurations for multi-dataset finetuning with large context length.}
\label{tab:wide_results_3}
\end{table}
By looking at Table~\ref{tab:wide_results_3}, we can see that configuration 2 performed best out of all datasets and configuration 1 performed worst.
% 4/6 individuellen Datensets wurde es besser bzw. hat sich nicht verschlechtert
% 7 Datensets wurden besser. Davon 2, auf denen auch trainiert wurde.

\section{Discussion}
The first research question we developed in the expose is ``Can we improve the performance of TabPFN-TS by finetuning on downstream tasks?''.
The results that we got by finetuning TabPFN-TS on all 33 datasets of GIFT-Eval suggest that finetuning can improve performance, but the effect strongly depends on dataset characteristics.

% Validation Loss nicht richtige Order bzw. Indikator, dass Validierung besser ist
We first examine the significance of the validation loss for improved evaluation results.
As mentioned in \ref{sec:results_finetuning}, we saw little to no correlation between the order of configurations regarding the validation loss and the order of them regarding the evaluation loss.
For example, in Figure~\ref{fig:eval_M_DENSE_H} we saw that the fifth configuration outperforms all of the other four configurations that were selected in order of validation loss.
Firstly, this discrepancy may be explained by the difference between validation loss (MSE) and evaluation metric (MASE).
Secondly, this may also be due to overfitting to the validation data.
Datasets with one time series often halve their validation loss but show worsening in evaluation (e.g., see results of finetuning with the dataset \textit{saugeenday/M} in the appendix in Table~\ref{tab:finetuning_saugeenday}).

By looking at Figure~\ref{fig:plot1_score_series}, we saw that the number of observations in one dataset appears to correlate with improved performance.
If a dataset has more than 100 thousand total observations, finetuning is more likely to improve the evaluation loss.
%Hier nochmal theoretischen Teil mit Paper
Results in Figure~\ref{fig:plot2_context_vs_series} further confirm this trend showing that finetuning on large context sizes and a large amount of time series tend to improve performance.
This suggests that not only the number of observations contributes to an improved finetuning run, but also the structure of the dataset itself does, by providing multiple time series with a certain amount of datapoints.
\newline
Time series datasets differ from standard tabular datasets in that the tables within a dataset stem from the same domain and share a common distribution.
A longer individual time series does not necessarily lead to better finetuning (with some exceptions).
However, providing multiple time series (and thus multiple tables) appears to be beneficial finetuning.
An example is weather forecasting: having observations from multiple weather stations within the same city provides more information, as different stations exhibit variation in their measurements while still following nearly the same underlying distribution.

%failure cases
There are some exceptions to this.
We identified two task characteristics that make finetuning harder: Noisy datasets and intermittent data.
If we take a look at the \textit{hospital} dataset that we also used as an example in Figure~\ref{fig:hospital_tabpfn_ts_pred}, we observe a slight downward trend but no clear periodicities.
Moreover, the series exhibits substantial random fluctuations, indicating a high noise level and making forecasting challenging.
This has been observed in prior work reporting that time series forecasting models have problems predicting on noisy datasets, as they were mostly trained on well-composed time series and have a hard time finding a schema in these data \citep{gupta2025evaluating}.
\newline
\begin{figure}[H]
    \centering
    \includegraphics[scale=1.0]{pictures/bizitobs_service_plot.png}
    \caption{Plotting of the first time series of the \textit{bizitobs\_service} dataset as example for intermittent data.}
    \label{fig:bizitobs_plotting}
\end{figure}
Other datasets show other characteristics that make it hard for TabPFN to train on.
Looking at~\ref{fig:bizitobs_plotting}, we can see that in this dataset, a signal emerges periodically, but not in the same amplitude every time.
Between those signals, the values of the time series are constantly at or near zero.
This type of data is called intermittent data.
\citet{KieferGrimmBaueretal_2021} showed that intermittent and lumpy time series remain challenging for forecasting models.
While deep learning models like Long Short-Term Memory (LSTM) have shown competitive performance, domain-specific statistical approaches such as Croston’s method regularly deliver better results, especially when dealing with intermittent demand.
This may partly explain why TabPFN was difficult to finetune on this dataset, as intermittent structure with sparse bursts and varying amplitudes may be hard to capture.
%- schlecht zu quantifizieren aber Datensätze mit Time Series die sehr "noisy"(also nicht vorausschaubar) sind funktionieren nicht so gut weil TabPFN kein Schema finden kann (hier vielleicht https://arxiv.org/abs/2501.00889 aber das Paper sieht bisschen komisch aus)
%- Wenn Signal nur sehr selten kommt und dazwischen das Signal "konstant" ist, lernt es auch schlecht (intermittent Daten) https://link.springer.com/article/10.1007/s10479-023-05447-7
%hier erklären
% especially datasets with challenging structure were bad at finetuning
Overall, these results show that finetuning TabPFN-TS on real-world time series data can improve forecasting performance, but only under certain conditions.
In particular, success depends strongly on dataset size, structure, and signal quality, while noisy or intermittent datasets remain challenging.

\iffalse
- immer mit Research-Frage anfangen
- finetuning on one dataset teuer weil viele Hyperparameter und nicht die eine Hyperparameter Config gefunden wurde
- dass der Validation Loss bei allen runtergeht aber die Evaluation nicht immer klappt lässt sich mit Overfitting auf den Validation Sets erklären
- Das sieht man vor allen Dingen bei den Datensets, die nur eine Zeitreihe haben -> Validation Loss wird manchmal halbiert, aber Evaluation viel schlechter
- Wenn das Signal nur sehr selten kommt und dazwischen das Signal "konstant" ist, lernt es auch schlecht (intermittent Daten) https://link.springer.com/article/10.1007/s10479-023-05447-7
- conclusion -> Es geht aber es gibt limitations durch Charakteristiken von Datensets und ihren Time Series
\fi

The question we investigate in our multi-dataset finetuning experiments is:
"Can we further improve performance by finetuning on multiple datasets similar to RealTabPFN \citep{Hollmann_2025}?"
The results suggest that multi-dataset finetuning can improve performance, although not every category we established equally helped train the model to perform better on our evaluation datasets.
In the upcoming section, we will assess the different categories and finally compare them to single finetuning.

Finetuning with the small context lengths category only led to worse results.
This can be observed not only in our evaluation datasets but also in almost all datasets we trained on, including those from the medium and large categories.
This result goes hand in hand with the results also obtained by \citet{Hollmann_2025}.
A possible reason for this is the lack of information we have due to the smaller context.
TabPFN is not able to identify underlying structures, thus generating a noisier gradient.
\newline
Another possibility is the differences regarding the characteristics of the time series.
Given that the context length is already small, it is also problematic that these inconsistencies occur.

In contrast, the medium category showed the most consistent improvements.
Based on the five configurations, TabPFN-TS improved on average on nearly all evaluation datasets.
This is likely due to a larger number of datasets in this category, as well as more equally distributed characteristics that make it easier for TabPFN to learn with.
\newline
This performance does not distribute equally across all five configurations; it applies only to the first two.
A factor that separates these two configurations from the other lower-performing ones is the learning rate.
The first two trained with a learning rate of 0.00005, while the other three trained with a learning rate of 0.0005.
This may suggest that a more conservative approach with lighter updates suits TabPFN finetuning better in this context length setting.

For the training run we did with datasets from the large context-length category, we also achieved improvements in performance, although they were more subtle than the ones we had with the medium context length.
It can also be observed that the small-context-length dataset evaluation resulted in performance degradation, whereas the evaluation run with the medium context-length dataset showed improved performance.
This reinforces our theory that, for small-context-length datasets, TabPFN does not achieve satisfying performance boosts after finetuning across multiple datasets with any context length.
\newline
\citet{Hollmann_2025} observed an increase in downstream accuracy with larger context sizes when finetuning TabPFN across multiple tabular datasets.
Despite also seeing a large increase in accuracy from small to medium context length, we did not see the same leap from medium to large context size.
This could be due to a different technical setup than the one used in their work, or to the fact that we finetuned using time-series data and the effects that result from it.
However, this effect cannot be conclusively explained within the scope of this work and requires further investigation.

These observations highlight that the effectiveness of finetuning is highly dependent on dataset characteristics and cannot be assumed to generalize uniformly across different time series settings.
% 2. Small context length results
% 3. Medium context length results
% 4. Big context length results
% 5. HP LR 0.00005 besser als 0.0005 und L2 SP lambda -> instabiler, lieber konservativeres Training

% 1. High-level summary
% 5. rolle der Datenheterogenität
% 6. Transfer zwischen Kategorien
% 7. limitations: wenige unabhängige Datensets, starker Datenset-Auswahl-Trainings-Bias

% 8. Conclusion: sinnvoll für Generalisierung, wirkt wie lightweight Pretraining


% Single besser für einzelne dafür Multi besser für Generalisierung

% Nur weil auf Datensets trainiert wird heißt es nicht dass es auch besser wird für Multi

% Multi funktioniert auf Medium Datensets am besten dann Big geht bisschen und Small geht gar nicht
% Aber das kann zum Teil auch an Datensets liegen auf denen es einfach besser funktioniert

% Hyperparameter kann man nicht so gut fassen weil es zwischen allen Läufen anders ist welche gut sind und wie lange sie brauchen
% Wenn Multi-Finetuning funktioniert dann hilft es auch zum Teil für Datensets die gar nicht im Fokus für das Training waren
% funktionieren also wie eine Art Prior zum Teil und deshalb wird für Time Series Data dass von real tabpfn zum Teil bestätigt
% Small: → „funktioniert gar nicht / Overfitting / kein Transfer“
%However, as can be seen in~\ref{tab:datasets} ett1/W also was a dataset we trained on.

% Medium: → „beste Generalisierung“
%lr vielleicht 0,00005 besser als 0.0005

% Big: → „stabil aber wenig Gain“

%limitations: „wenig eval Datensets, weil viele für Training genutzt wurden“