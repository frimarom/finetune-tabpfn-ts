\chapter{Preliminaries}
\section{PFN: Prior Fitted Network}
- hier noch ppd erwähnen dass die approximiert wird

A prior is a generative process that samples artificial tabular based on different hypotheses.

    $p(D)= \mathbb{E}_{}$
Where $\phi$ is said hypotheses which is sampled with $\phi \sim p(\phi)$ and normally represents either the whole algorithm that generates the data or the hyperparameters that are used to generate the data.
- prior essential for PFNs
- prior hyperparameter consists of distributions instead of point estimates
- prior data is either sampled from SCM prior or BNN Prior


SCMs for TabPFN
An SCM is a collection of $k$ structural assignments $Z:=(z_1, ..., z_k)$ which are represented by nodes in a directed acyclic graph where $z_i = f_i(z_{PA\mathcal{G}(i)}, \epsilon)$.
$f_i$ is a (nonlinear) deterministic function and $\epsilon_i$ is noise variable.
To generate a new artificial dataset
\section{Time Series}
-Time series mit seasonal trend component etc erklären
\section{Finetuning}
\section{Continued Pretraining}
